# v0.05 自动保存 + localStorage 结构整理 — 实现计划

> **面向 AI 代理的工作者：** 使用 subagent-driven-development 或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 整理 localStorage 三层结构（config/schema/document）并实现变化防抖 + 定时兜底的自动保存机制。

**架构：** 新增 `logic-migration.js` 处理一次性迁移 + 新 key 读写，新增 `logic-autoSave.js` 处理自动保存核心逻辑，新增 `AutoSaveIndicator.vue` 显示保存状态。逐步改造现有文件的 localStorage 读写点。

**技术栈：** 原生 JS (ES Module) + Vue 3 + Pinia

---

## 文件清单

### 新增文件

| 文件 | 职责 |
|------|------|
| `js/logic/logic-migration.js` | localStorage 迁移 + 新三层结构常量 + 读写辅助函数 |
| `js/logic/logic-autoSave.js` | 自动保存核心：防抖/兜底定时器、beforeunload、状态管理 |
| `components/AutoSaveIndicator.vue` | 状态指示器组件 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `js/logic/logic-storyTypes.js` | 所有 localStorage 读写改为迁移模块的新辅助函数 |
| `js/ui/ui-chapterView.js` | `storyeditor_chapter_cols` → `storyeditor_config.chapterCols` |
| `js/ui/ui-labelManager.js` | `storyeditor_labels` → `storyeditor_config.labels` |
| `js/ui/ui-settingsPanel.js` | `storyeditor_settings` → `storyeditor_config`，导出格式同步 |
| `components/Settings/SettingsPanel.vue` | 导出/导入格式同步为新结构 |
| `js/main.js` | 启动时执行迁移 + 自动保存恢复弹窗 + 注册 autoSave |
| `js/barrel.js` | 导出新模块 |
| `stores/storyStore.js` | 新增 `autoSaveStatus` 响应式状态 |
| `components/App.vue` | 引入 AutoSaveIndicator |
| `components/layout/ActivityBar.vue` | 底部添加状态指示器容器 |

---

## 任务

### 任务 1：创建 `js/logic/logic-migration.js`

**文件：** 创建 `js/logic/logic-migration.js`

**说明：** 定义新的三层结构 key 常量，提供读写辅助函数，实现从旧 key 到新结构的一次性迁移。

- [ ] **步骤 1：编写 `logic-migration.js`**

```javascript
// ==========================================
// logic-migration.js — localStorage 数据结构迁移
// 定义三层结构 key + 读写辅助函数
// ==========================================

// ---- Key 常量 ----
export const CONFIG_KEY = 'storyeditor_config';
export const SCHEMA_KEY = 'storyeditor_schema';
export const DOCUMENT_KEY = 'storyeditor_document';
export const MIGRATION_FLAG = '_migrated_v05';

// ---- 旧 key 常量（仅迁移用） ----
const OLD_KEYS = {
  settings: 'storyeditor_settings',
  labels: 'storyeditor_labels',
  chapterCols: 'storyeditor_chapter_cols',
  templates: 'storyeditor_templates',
  deletedTemplates: 'storyeditor_deleted_templates',
  templateKeys: 'storyeditor_template_keys',
  structs: 'storyeditor_structs',
};

// ---- 读写辅助函数 ----

export function readConfig() {
  try {
    const raw = localStorage.getItem(CONFIG_KEY);
    return raw ? JSON.parse(raw) : null;
  } catch { return null; }
}

export function writeConfig(data) {
  localStorage.setItem(CONFIG_KEY, JSON.stringify({
    meta: { version: 1, updatedAt: new Date().toLocaleString('zh-CN') },
    ...data
  }));
}

export function readSchema() {
  try {
    const raw = localStorage.getItem(SCHEMA_KEY);
    return raw ? JSON.parse(raw) : null;
  } catch { return null; }
}

export function writeSchema(data) {
  localStorage.setItem(SCHEMA_KEY, JSON.stringify({
    meta: { version: 1, updatedAt: new Date().toLocaleString('zh-CN') },
    ...data
  }));
}

export function readDocument() {
  try {
    const raw = localStorage.getItem(DOCUMENT_KEY);
    return raw ? JSON.parse(raw) : null;
  } catch { return null; }
}

export function writeDocument(data) {
  localStorage.setItem(DOCUMENT_KEY, JSON.stringify(data));
}

export function clearDocument() {
  localStorage.removeItem(DOCUMENT_KEY);
}

/** 检测旧 key 是否仍存在（判断是否需要迁移） */
function hasOldKeys() {
  return localStorage.getItem(OLD_KEYS.settings) !== null
      || localStorage.getItem(OLD_KEYS.chapterCols) !== null;
}

/** 执行一次性迁移：读取旧 key → 写入新结构 → 删除旧 key */
export function runMigration() {
  if (localStorage.getItem(MIGRATION_FLAG) === 'true') return false;

  let migrated = false;

  // ---- 迁移第 1 层：storyeditor_config ----
  const oldSettings = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.settings) || 'null'); }
    catch { return null; }
  })();
  const oldLabels = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.labels) || 'null'); }
    catch { return null; }
  })();
  const oldCols = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.chapterCols) || 'null'); }
    catch { return null; }
  })();

  if (oldSettings || oldLabels || oldCols) {
    const config = {};
    if (oldSettings) {
      config.theme = oldSettings.theme || 'dark';
      config.fontSize = oldSettings.fontSize || 16;
      config.labelColorMode = oldSettings.labelColor || oldSettings.labelColorMode || 'type';
    }
    if (oldLabels) config.labels = oldLabels;
    if (oldCols) config.chapterCols = oldCols;
    writeConfig(config);
    migrated = true;
  }

  // ---- 迁移第 2 层：storyeditor_schema ----
  const oldTemplates = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.templates) || 'null'); }
    catch { return null; }
  })();
  const oldDeleted = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.deletedTemplates) || 'null'); }
    catch { return null; }
  })();
  const oldKeys = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.templateKeys) || 'null'); }
    catch { return null; }
  })();
  const oldStructs = (() => {
    try { return JSON.parse(localStorage.getItem(OLD_KEYS.structs) || 'null'); }
    catch { return null; }
  })();

  if (oldTemplates || oldDeleted || oldKeys || oldStructs) {
    const schema = {};
    if (oldTemplates) schema.templates = oldTemplates;
    if (oldDeleted) schema.deletedTemplates = oldDeleted;
    if (oldKeys) schema.templateKeys = oldKeys;
    if (oldStructs) schema.structs = oldStructs;
    writeSchema(schema);
    migrated = true;
  }

  // ---- 删除所有旧 key ----
  if (migrated) {
    for (const key of Object.values(OLD_KEYS)) {
      localStorage.removeItem(key);
    }
    localStorage.setItem(MIGRATION_FLAG, 'true');
  }

  return migrated;
}
```

- [ ] **步骤 2：验证模块无报错**

运行：`node -e "const m = require('./js/logic/logic-migration.js'); console.log('ok')"` — 或用浏览器 console 确认 ES module 可导入。

---

### 任务 2：更新 `js/logic/logic-storyTypes.js` — 使用新存储结构

**文件：** 修改 `js/logic/logic-storyTypes.js`

**说明：** 将所有直接引用旧 localStorage key 的常量改为通过 `logic-migration.js` 的辅助函数读写。涉及：templates、labels、structs、templateKeys、deletedTemplates 的读写。

- [ ] **步骤 1：导入迁移模块并替换各 key 的读写函数**

在文件顶部引入：
```javascript
import { readConfig, writeConfig, readSchema, writeSchema } from './logic-migration.js';
```

然后修改以下代码块：

**(a) 替换 TEMPLATE_KEY / loadTemplates / saveTemplates**

```diff
- const TEMPLATE_KEY = 'storyeditor_templates';
  function loadTemplates() {
      try {
-         const saved = JSON.parse(localStorage.getItem(TEMPLATE_KEY) || '{}');
+         const schema = readSchema();
+         const saved = schema?.templates || {};
          const defaults = getDefaultTemplates();
          return { ...defaults, ...saved };
      }
-     catch { return { ...getDefaultTemplates() }; }
+     catch { return { ...getDefaultTemplates() }; }
  }
```

```diff
- const DELETED_KEY = 'storyeditor_deleted_templates';
  function loadDeletedTemplates() {
-     try { return JSON.parse(localStorage.getItem(DELETED_KEY) || '[]'); }
+     try { const s = readSchema(); return s?.deletedTemplates || []; }
      catch { return []; }
  }
  function saveDeletedTemplates(arr) {
-     localStorage.setItem(DELETED_KEY, JSON.stringify([...new Set(arr)]));
+     const s = readSchema() || {};
+     s.deletedTemplates = [...new Set(arr)];
+     writeSchema(s);
  }
```

```diff
- const KEYS_KEY = 'storyeditor_template_keys';
  function loadTemplateKeys() {
-     try { return JSON.parse(localStorage.getItem(KEYS_KEY) || '{}'); }
+     try { const s = readSchema(); return s?.templateKeys || {}; }
      catch { return {}; }
  }
  function saveTemplateKeys(keys) {
-     localStorage.setItem(KEYS_KEY, JSON.stringify(keys));
+     const s = readSchema() || {};
+     s.templateKeys = keys;
+     writeSchema(s);
  }
```

```diff
  function saveTemplates(tpls, deletedKeys = []) {
      // ... 处理逻辑不变 ...
-     localStorage.setItem(TEMPLATE_KEY, JSON.stringify(toSave));
+     const s = readSchema() || {};
+     s.templates = toSave;
      if (deletedKeys.length > 0) {
          const existing = loadDeletedTemplates();
          saveDeletedTemplates([...new Set([...existing, ...deletedKeys])]);
      }
+     writeSchema(s);
  }
```

**(b) 替换 LABEL_STORAGE_KEY**

```diff
- const LABEL_STORAGE_KEY = 'storyeditor_labels';
  function loadLabels() {
-     try { return JSON.parse(localStorage.getItem(LABEL_STORAGE_KEY) || '{}'); } catch { return {}; }
+     try { const c = readConfig(); return c?.labels || {}; } catch { return {}; }
  }
  function saveLabel(key, label) {
      const all = loadLabels();
-     all[key] = label; localStorage.setItem(LABEL_STORAGE_KEY, JSON.stringify(all));
+     all[key] = label;
+     const c = readConfig() || {};
+     c.labels = all;
+     writeConfig(c);
  }
```

**(c) 替换 STRUCTS_KEY**

```diff
- const STRUCTS_KEY = 'storyeditor_structs';
  function loadStructs() {
      try {
-         const saved = JSON.parse(localStorage.getItem(STRUCTS_KEY) || '[]');
+         const s = readSchema();
+         const saved = s?.structs || [];
          // ... 后续不变 ...
      } catch { return JSON.parse(JSON.stringify(DEFAULT_STRUCTS)); }
  }
  function saveStructs(structs) {
-     localStorage.setItem(STRUCTS_KEY, JSON.stringify(structs));
+     const s = readSchema() || {};
+     s.structs = structs;
+     writeSchema(s);
  }
```

完整替换后的代码（精确 SEARCH/REPLACE 块）：

**对于 TEMPLATE_KEY + loadTemplates：**
```javascript
// 替换前 (line 112-122):
const TEMPLATE_KEY = 'storyeditor_templates';
const LABEL_STORAGE_KEY = 'storyeditor_labels';

export function loadTemplates() {
    try {
        const saved = JSON.parse(localStorage.getItem(TEMPLATE_KEY) || '{}');
        const defaults = getDefaultTemplates();
        return { ...defaults, ...saved };
    }
    catch { return { ...getDefaultTemplates() }; }
}
```

→ 替换为：

```javascript
export function loadTemplates() {
    try {
        const schema = readSchema();
        const saved = schema?.templates || {};
        const defaults = getDefaultTemplates();
        return { ...defaults, ...saved };
    }
    catch { return { ...getDefaultTemplates() }; }
}
```

**对于 DELETED_KEY：**
```javascript
// 替换前 (line 124-133):
const DELETED_KEY = 'storyeditor_deleted_templates';

function loadDeletedTemplates() {
    try { return JSON.parse(localStorage.getItem(DELETED_KEY) || '[]'); }
    catch { return []; }
}

function saveDeletedTemplates(arr) {
    localStorage.setItem(DELETED_KEY, JSON.stringify([...new Set(arr)]));
}
```

→ 替换为：

```javascript
function loadDeletedTemplates() {
    try { const s = readSchema(); return s?.deletedTemplates || []; }
    catch { return []; }
}

function saveDeletedTemplates(arr) {
    const s = readSchema() || {};
    s.deletedTemplates = [...new Set(arr)];
    writeSchema(s);
}
```

**对于 KEYS_KEY：**
```javascript
// 替换前 (line 135-143):
const KEYS_KEY = 'storyeditor_template_keys';

export function loadTemplateKeys() {
    try { return JSON.parse(localStorage.getItem(KEYS_KEY) || '{}'); }
    catch { return {}; }
}

export function saveTemplateKeys(keys) {
    localStorage.setItem(KEYS_KEY, JSON.stringify(keys));
}
```

→ 替换为：

```javascript
export function loadTemplateKeys() {
    try { const s = readSchema(); return s?.templateKeys || {}; }
    catch { return {}; }
}

export function saveTemplateKeys(keys) {
    const s = readSchema() || {};
    s.templateKeys = keys;
    writeSchema(s);
}
```

**对于 saveTemplates：**
```javascript
// 替换前 (line 155-169):
export function saveTemplates(tpls, deletedKeys = []) {
    const toSave = {};
    const defaults = getDefaultTemplates();
    for (const [k, v] of Object.entries(tpls)) {
        if (JSON.stringify(defaults[k]) !== JSON.stringify(v)) toSave[k] = v;
    }
    localStorage.setItem(TEMPLATE_KEY, JSON.stringify(toSave));
    if (deletedKeys.length > 0) {
        const existing = loadDeletedTemplates();
        saveDeletedTemplates([...new Set([...existing, ...deletedKeys])]);
    }
}
```

→ 替换为：

```javascript
export function saveTemplates(tpls, deletedKeys = []) {
    const toSave = {};
    const defaults = getDefaultTemplates();
    for (const [k, v] of Object.entries(tpls)) {
        if (JSON.stringify(defaults[k]) !== JSON.stringify(v)) toSave[k] = v;
    }
    const s = readSchema() || {};
    s.templates = toSave;
    if (deletedKeys.length > 0) {
        const existing = loadDeletedTemplates();
        s.deletedTemplates = [...new Set([...existing, ...deletedKeys])];
    }
    writeSchema(s);
}
```

**对于 LABEL_STORAGE_KEY + loadLabels + saveLabel：**
```javascript
// 替换前:
const LABEL_STORAGE_KEY = 'storyeditor_labels';

export function loadLabels() {
    try { return JSON.parse(localStorage.getItem(LABEL_STORAGE_KEY) || '{}'); } catch { return {}; }
}
export function saveLabel(key, label) { const all = loadLabels(); all[key] = label; localStorage.setItem(LABEL_STORAGE_KEY, JSON.stringify(all)); }
```

→ 替换为：

```javascript
export function loadLabels() {
    try { const c = readConfig(); return c?.labels || {}; } catch { return {}; }
}
export function saveLabel(key, label) {
    const all = loadLabels();
    all[key] = label;
    const c = readConfig() || {};
    c.labels = all;
    writeConfig(c);
}
```

**对于 STRUCTS_KEY + loadStructs + saveStructs：**
```javascript
// 替换前:
const STRUCTS_KEY = 'storyeditor_structs';

const DEFAULT_STRUCTS = [
    { id: 'i18n', label: '多语言文本', match: { type: 'struct', marker: 'zh' }, fields: ['zh', 'en'] }
];

export function loadStructs() {
    try {
        const saved = JSON.parse(localStorage.getItem(STRUCTS_KEY) || '[]');
        if (saved.length === 0) return JSON.parse(JSON.stringify(DEFAULT_STRUCTS));
        for (const s of saved) {
            if (s.match?.type === 'struct' && s.match.marker) {
                if (!s.fields.includes(s.match.marker)) {
                    s.fields.unshift(s.match.marker);
                }
            }
        }
        return saved;
    } catch { return JSON.parse(JSON.stringify(DEFAULT_STRUCTS)); }
}

export function saveStructs(structs) {
    localStorage.setItem(STRUCTS_KEY, JSON.stringify(structs));
}
```

→ 替换为：

```javascript
const DEFAULT_STRUCTS = [
    { id: 'i18n', label: '多语言文本', match: { type: 'struct', marker: 'zh' }, fields: ['zh', 'en'] }
];

export function loadStructs() {
    try {
        const schema = readSchema();
        const saved = schema?.structs || [];
        if (saved.length === 0) return JSON.parse(JSON.stringify(DEFAULT_STRUCTS));
        for (const s of saved) {
            if (s.match?.type === 'struct' && s.match.marker) {
                if (!s.fields.includes(s.match.marker)) {
                    s.fields.unshift(s.match.marker);
                }
            }
        }
        return saved;
    } catch { return JSON.parse(JSON.stringify(DEFAULT_STRUCTS)); }
}

export function saveStructs(structs) {
    const s = readSchema() || {};
    s.structs = structs;
    writeSchema(s);
}
```

- [ ] **步骤 2：确认 `logic-storyTypes.js` 的 import 添加正确**

确保文件顶部新增：
```javascript
import { readConfig, writeConfig, readSchema, writeSchema } from './logic-migration.js';
```

注意：`barrel.js` 不导出 `readConfig`/`writeConfig`/`readSchema`/`writeSchema`，这里直接 import 文件路径。

---

### 任务 3：更新 `js/ui/ui-chapterView.js` — 使用新 config 结构

**文件：** 修改 `js/ui/ui-chapterView.js`

**说明：** `storyeditor_chapter_cols` → 通过 `readConfig().chapterCols` 读写

- [ ] **步骤 1：替换 loadColumnConfig 和 saveColumnConfig**

```diff
+ import { readConfig, writeConfig } from '../logic/logic-migration.js';

  function loadColumnConfig() {
-     try { return JSON.parse(localStorage.getItem('storyeditor_chapter_cols') || '["speaker","text"]'); }
+     try { const c = readConfig(); return c?.chapterCols || ['speaker', 'text']; }
      catch { return ['speaker', 'text']; }
  }

  function saveColumnConfig(cols) {
-     localStorage.setItem('storyeditor_chapter_cols', JSON.stringify(cols));
+     const c = readConfig() || {};
+     c.chapterCols = cols;
+     writeConfig(c);
  }
```

---

### 任务 4：更新 `js/ui/ui-labelManager.js` — 使用新 config 结构

**文件：** 修改 `js/ui/ui-labelManager.js`

**说明：** `storyeditor_labels` 的直接读写 → 已通过 `loadLabels()` / `saveLabel()` 封装，但第 69 行有直接 `removeItem('storyeditor_labels')`，需要改为新方式。

- [ ] **步骤 1：替换 `removeItem('storyeditor_labels')`**

第 67-69 行：
```diff
  const ok = await showConfirm('确定重置所有字段标签为默认值吗？');
  if (ok) {
-     localStorage.removeItem('storyeditor_labels');
+     const c = readConfig() || {};
+     delete c.labels;
+     writeConfig(c);
      store._emit();
      renderLabelList();
  }
```

第 104-108 行（删除单个标签时）：
```diff
  const all = loadLabels();
  delete all[key];
- localStorage.setItem('storyeditor_labels', JSON.stringify(all));
+ const c = readConfig() || {};
+ c.labels = all;
+ writeConfig(c);
```

第 120-122 行（同样的删除逻辑）：
```diff
  const all = loadLabels();
  delete all[key];
- localStorage.setItem('storyeditor_labels', JSON.stringify(all));
+ const c = readConfig() || {};
+ c.labels = all;
+ writeConfig(c);
```

在文件顶部添加 import：
```diff
+ import { readConfig, writeConfig } from '../logic/logic-migration.js';
```

---

### 任务 5：更新 `js/ui/ui-settingsPanel.js`— 使用新 config 结构

**文件：** 修改 `js/ui/ui-settingsPanel.js`

**说明：** `STORAGE_KEY = 'storyeditor_settings'` → 改用 `readConfig()/writeConfig()`，导出/导入格式同步更新为新结构。

- [ ] **步骤 1：替换 STORAGE_KEY 及相关函数**

```diff
+ import { readConfig, writeConfig, readSchema, writeSchema } from '../logic/logic-migration.js';
- const STORAGE_KEY = 'storyeditor_settings';

  function loadSettings() {
-     try { const saved = localStorage.getItem(STORAGE_KEY); if (saved) return JSON.parse(saved); } catch {}
+     try { const c = readConfig(); if (c) return { theme: c.theme, fontSize: c.fontSize, labelColor: c.labelColorMode }; } catch {}
      return { theme: 'dark', fontSize: 16, labelColor: 'type' };
  }

  function saveSettings(s) {
-     localStorage.setItem(STORAGE_KEY, JSON.stringify(s));
+     const c = readConfig() || {};
+     c.theme = s.theme;
+     c.fontSize = s.fontSize;
+     c.labelColorMode = s.labelColor;
+     writeConfig(c);
  }
```

- [ ] **步骤 2：更新导出配置格式**

第 526-550 行（导出 `btn-export-config`）：
```diff
  const exportBtn = document.getElementById('btn-export-config');
  if (exportBtn) {
      exportBtn.addEventListener('click', () => {
          const config = {
              meta: { version: 1, exportedAt: new Date().toISOString() },
-             editor: {
-                 settings: loadSettings(),
-                 chapterCols: (() => { try { return JSON.parse(localStorage.getItem('storyeditor_chapter_cols') || '["speaker","text"]'); } catch { return ['speaker', 'text']; } })()
-             },
-             custom: {
-                 templates: loadEffectiveTemplates(),
-                 deletedTemplates: (() => { try { return JSON.parse(localStorage.getItem('storyeditor_deleted_templates') || '[]'); } catch { return []; } })(),
-                 templateKeys: loadTemplateKeys(),
-                 structs: loadStructs(),
-                 labels: loadLabels()
-             }
+             config: readConfig() || {},
+             schema: readSchema() || {}
          };
          // ... blob 导出逻辑不变 ...
      });
  }
```

- [ ] **步骤 3：更新导入配置格式**

第 552-620 行（导入 `btn-import-config`）：
```diff
  const importBtn = document.getElementById('btn-import-config');
  if (importBtn) {
      // ...
      reader.onload = (e) => {
          try {
              const data = JSON.parse(e.target.result);
              let count = 0;

-             if (data.editor && data.custom) {
-                 // 新版结构化格式
-                 if (data.editor.settings) { localStorage.setItem('storyeditor_settings', JSON.stringify(data.editor.settings)); count++; }
-                 if (data.editor.chapterCols) { localStorage.setItem('storyeditor_chapter_cols', JSON.stringify(data.editor.chapterCols)); count++; }
-                 if (data.custom.templates) { localStorage.setItem('storyeditor_templates', JSON.stringify(data.custom.templates)); count++; }
-                 if (data.custom.deletedTemplates) { localStorage.setItem('storyeditor_deleted_templates', JSON.stringify(data.custom.deletedTemplates)); count++; }
-                 if (data.custom.templateKeys) { localStorage.setItem('storyeditor_template_keys', JSON.stringify(data.custom.templateKeys)); count++; }
-                 if (data.custom.structs) { localStorage.setItem('storyeditor_structs', JSON.stringify(data.custom.structs)); count++; }
-                 if (data.custom.labels) { localStorage.setItem('storyeditor_labels', JSON.stringify(data.custom.labels)); count++; }
+             if (data.config) { writeConfig(data.config); count++; }
+             if (data.schema) { writeSchema(data.schema); count++; }

-             } else {
-                 // 兼容旧版平铺格式
-                 for (const [key, val] of Object.entries(data)) {
-                     if (key.startsWith('storyeditor_')) { localStorage.setItem(key, JSON.stringify(val)); count++; }
-                 }
-             }
+             // 兼容旧格式（平铺 storyeditor_*）
+             if (!data.config && !data.schema) {
+                 for (const [key, val] of Object.entries(data)) {
+                     if (key.startsWith('storyeditor_')) {
+                         localStorage.setItem(key, JSON.stringify(val));
+                         count++;
+                     }
+                 }
+             }

              showAlert(`导入成功！已恢复 ${count} 项配置。`);
              initSettings();
              renderSettingsPanel();
              store._emit();
          } catch (err) { showAlert('导入失败：配置文件格式错误'); }
      };
      // ...
  }
```

- [ ] **步骤 4：更新重置按钮**

第 496-505 行：
```diff
  resetBtn.addEventListener('click', () => {
-     localStorage.removeItem(STORAGE_KEY);
+     localStorage.removeItem('storyeditor_config');
      const defaults = { theme: 'dark', fontSize: 16, labelColor: 'type' };
      saveSettings(defaults);
      applySettings(defaults);
      renderSettingsPanel();
  });
```

---

### 任务 6：更新 `components/Settings/SettingsPanel.vue` — 同步导出/导入格式

**文件：** 修改 `components/Settings/SettingsPanel.vue`

**说明：** 这个 Vue 组件也有自己的导出/导入逻辑，需要同步为新结构。

- [ ] **步骤 1：更新 `doExport` 函数**

第 201-217 行：
```diff
+ import { readConfig, writeConfig, readSchema, writeSchema } from '../../js/logic/logic-migration.js';

  function doExport(...args) {
      const config = {
          meta: { version: 1, exportedAt: new Date().toISOString() },
-         editor: { settings: loadSettings(), chapterCols: JSON.parse(localStorage.getItem('storyeditor_chapter_cols') || '["speaker","text"]') },
-         custom: {
-             templates: loadEffectiveTemplates(),
-             deletedTemplates: JSON.parse(localStorage.getItem('storyeditor_deleted_templates') || '[]'),
-             templateKeys: loadTemplateKeys(),
-             structs: loadStructs(),
-             labels: loadLabels()
-         }
+         config: readConfig() || {},
+         schema: readSchema() || {}
      };
      // ... 导出 blob 逻辑不变 ...
  }
```

- [ ] **步骤 2：更新 `doImport` 函数**

第 226-249 行：
```diff
          try {
              const d = JSON.parse(e.target.result); let c = 0
-             const set = (k, v) => { localStorage.setItem(k, JSON.stringify(v)); c++ }
-             if (d.editor && d.custom) {
-                 if (d.editor.settings) set('storyeditor_settings', d.editor.settings)
-                 if (d.editor.chapterCols) set('storyeditor_chapter_cols', d.editor.chapterCols)
-                 if (d.custom.templates) set('storyeditor_templates', d.custom.templates)
-                 if (d.custom.deletedTemplates) set('storyeditor_deleted_templates', d.custom.deletedTemplates)
-                 if (d.custom.templateKeys) set('storyeditor_template_keys', d.custom.templateKeys)
-                 if (d.custom.structs) set('storyeditor_structs', d.custom.structs)
-                 if (d.custom.labels) set('storyeditor_labels', d.custom.labels)
-             } else {
-                 for (const [k, v] of Object.entries(d)) { if (k.startsWith('storyeditor_')) set(k, v) }
-             }
+             if (d.config) { writeConfig(d.config); c++ }
+             if (d.schema) { writeSchema(d.schema); c++ }
+             // 兼容旧格式
+             if (!d.config && !d.schema) {
+                 for (const [k, v] of Object.entries(d)) {
+                     if (k.startsWith('storyeditor_')) {
+                         localStorage.setItem(k, JSON.stringify(v)); c++
+                     }
+                 }
+             }
              // ... 后续不变 ...
```

- [ ] **步骤 3：更新 `doReset` 函数**

第 251-258 行：
```diff
  function doReset() {
-   localStorage.removeItem('storyeditor_settings')
+   localStorage.removeItem('storyeditor_config')
    Object.assign(settings, { ...defaults })
    applyTheme(defaults.theme)
    document.documentElement.style.setProperty('--font-size-base', defaults.fontSize + 'px')
    document.documentElement.dataset.labelColor = defaults.labelColor
-   localStorage.setItem('storyeditor_settings', JSON.stringify(defaults))
+   writeConfig({ theme: defaults.theme, fontSize: defaults.fontSize, labelColorMode: defaults.labelColor })
  }
```

- [ ] **步骤 4：移除不再需要的 import**

```diff
  import { loadLabels, getLanguages, loadStructs, loadEffectiveTemplates, loadTemplateKeys,
           loadTemplates, saveTemplates, getContextsConfig, getContextKeys,
           addStructField, removeStructField, deleteStruct } from '../../js/logic/logic-storyTypes.js'
```

`loadEffectiveTemplates`、`loadTemplateKeys`、`loadLabels` 只被 `doExport` 和 `doImport` 使用，现在已移除，可以去掉这些 import（但保留其他仍然使用的）。

---

### 任务 7：创建 `js/logic/logic-autoSave.js`

**文件：** 创建 `js/logic/logic-autoSave.js`

**说明：** 自动保存核心模块。管理防抖定时器（10s）+ 兜底定时器（60s）+ beforeunload 立即保存。暴露状态供 UI 层消费。

- [ ] **步骤 1：编写完整的 autoSave 模块**

```javascript
// ==========================================
// logic-autoSave.js — 自动保存核心逻辑
// 纯数据层，不依赖 UI
// ==========================================

import { readDocument, writeDocument, clearDocument } from './logic-migration.js';

const DEBOUNCE_MS = 10000;  // 防抖间隔 10s
const HEARTBEAT_MS = 60000; // 兜底检查间隔 60s

let _debounceTimer = null;
let _heartbeatTimer = null;
let _lastSavedVersion = -1;
let _savedVersion = -1;
let _statusListeners = [];
let _storeRef = null;
let _fileName = '';

// ---- 状态枚举 ----
export const Status = {
  IDLE: 'idle',
  SAVING: 'saving',
  SAVED: 'saved',
  ERROR: 'error',
};

let _currentStatus = Status.IDLE;

function setStatus(status) {
  if (_currentStatus === status) return;
  _currentStatus = status;
  _statusListeners.forEach(fn => fn(status));
}

export function getStatus() {
  return _currentStatus;
}

export function onStatusChange(fn) {
  _statusListeners.push(fn);
  return () => {
    _statusListeners = _statusListeners.filter(f => f !== fn);
  };
}

// ---- 核心保存逻辑 ----

function performSave() {
  if (!_storeRef) return;
  setStatus(Status.SAVING);

  try {
    const chapter = _storeRef.chapter;
    if (!chapter) { setStatus(Status.IDLE); return; }

    const data = {
      meta: {
        version: 1,
        fileName: _fileName || 'untitled.json',
        savedAt: Date.now(),
        displayTime: new Date().toLocaleString('zh-CN')
      },
      data: JSON.parse(JSON.stringify(chapter))
    };

    writeDocument(data);
    _savedVersion = _storeRef._dataVersion;
    _lastSavedVersion = _savedVersion;

    setStatus(Status.SAVED);

    // 3 秒后自动回到 IDLE（仅当版本没再变化时）
    clearTimeout(_debounceTimer);
    _debounceTimer = setTimeout(() => {
      if (_storeRef && _storeRef._dataVersion === _savedVersion) {
        setStatus(Status.IDLE);
      }
    }, 3000);

  } catch (err) {
    console.warn('[AutoSave] 保存失败:', err);
    setStatus(Status.ERROR);
  }
}

// ---- 防抖通知 ----

export function notifyChange() {
  // 重置防抖计时器
  clearTimeout(_debounceTimer);
  _debounceTimer = setTimeout(() => {
    performSave();
  }, DEBOUNCE_MS);
}

// ---- 兜底心跳 ----

function heartbeat() {
  if (!_storeRef) return;
  const currentVersion = _storeRef._dataVersion;
  if (currentVersion > _savedVersion) {
    performSave();
  }
}

// ---- 生命周期 ----

export function start(store, fileName = '') {
  _storeRef = store;
  _fileName = fileName;
  _savedVersion = store._dataVersion;

  // 兜底定时器
  clearInterval(_heartbeatTimer);
  _heartbeatTimer = setInterval(heartbeat, HEARTBEAT_MS);

  // beforeunload 立即保存
  window.addEventListener('beforeunload', onBeforeUnload);
}

export function stop() {
  clearTimeout(_debounceTimer);
  clearInterval(_heartbeatTimer);
  window.removeEventListener('beforeunload', onBeforeUnload);
  _storeRef = null;
  _currentStatus = Status.IDLE;
  _savedVersion = -1;
}

function onBeforeUnload() {
  if (_storeRef && _storeRef._dataVersion > _savedVersion) {
    performSave();
  }
}

// ---- 恢复检测 ----

export function hasSavedDocument() {
  return readDocument() !== null;
}

export function getSavedDocument() {
  return readDocument();
}

export function discardSavedDocument() {
  clearDocument();
  _savedVersion = -1;
  _lastSavedVersion = -1;
}

// ---- 导出 JSON 后清除 ----

export function onExported() {
  discardSavedDocument();
}

// ---- 设置文件名 ----

export function setFileName(name) {
  _fileName = name;
}
```

---

### 任务 8：更新 `stores/storyStore.js` — 新增 autoSaveStatus 状态

**文件：** 修改 `stores/storyStore.js`

**说明：** 新增 `autoSaveStatus` 响应式状态，关联 `logic-autoSave.js` 的状态变更回调。

- [ ] **步骤 1：新增 autoSaveStatus 状态**

```diff
  import { ref, shallowRef } from 'vue'
  import { store as original } from '../js/logic/logic-storyStore.js'
  import { resolveTemplateContext, createNodeFromTemplate } from '../js/logic/logic-storyTypes.js'
+ import { getStatus, onStatusChange } from '../js/logic/logic-autoSave.js'

  export const useStoryStore = defineStore('story', () => {
    // ---- 响应式状态 ----
    const chapter = shallowRef(original.chapter)
    const currentPath = ref([...original.currentPath])
    const selectedId = ref(original.selectedId)
    const dataVersion = ref(original._dataVersion)
+   const autoSaveStatus = ref(getStatus())  // 'idle' | 'saving' | 'saved' | 'error'

    // ---- 同步函数 ----
    function sync() {
      chapter.value = original.chapter
      currentPath.value = [...original.currentPath]
      selectedId.value = original.selectedId
      dataVersion.value = original._dataVersion
    }

    // 监听原始 store 变更
    original.onChange(() => { sync() })

+   // 监听 auto-save 状态变更
+   onStatusChange((status) => { autoSaveStatus.value = status })

    // ---- 工具方法 ----
    // ... 其余不变 ...

    return {
      // 状态
-     chapter, currentPath, selectedId, dataVersion,
+     chapter, currentPath, selectedId, dataVersion, autoSaveStatus,
      // ...
    }
  })
```

---

### 任务 9：更新 `js/main.js` — 迁移 + 恢复 + autoSave 注册

**文件：** 修改 `js/main.js`

**说明：** 启动流程中加入：迁移旧数据 → 检查自动保存恢复 → 注册 autoSave。

- [ ] **步骤 1：改造 main.js**

```diff
  import { createApp } from 'vue';
  import { createPinia } from 'pinia';
  import App from '../components/App.vue';
  import { store, io, initUI, loadContentConfig, showAlert } from './barrel.js';
+ import { runMigration } from './logic/logic-migration.js';
+ import { start, notifyChange, hasSavedDocument, getSavedDocument, discardSavedDocument } from './logic/logic-autoSave.js';
+ import { showConfirm } from '../components/base/useDialog.js';

  // step 1: 挂载 Vue 根组件 + Pinia
  const app = createApp(App);
  app.use(createPinia());
  app.mount('#app');

+ // step 1.5: 执行 localStorage 迁移（旧 key → 新三层结构）
+ runMigration();

+ // step 1.6: 检查自动保存数据，询问是否恢复
+ (async function checkAutoSave() {
+   if (hasSavedDocument()) {
+     const saved = getSavedDocument();
+     const fileName = saved?.meta?.fileName || '未命名';
+     const time = saved?.meta?.displayTime || '未知';
+     const ok = await showConfirm(`检测到未保存的编辑数据：\n文件：${fileName}\n最后编辑时间：${time}\n\n是否恢复？`);
+     if (ok) {
+       store.loadChapter(saved.data);
+       showAlert('已恢复自动保存的数据');
+     }
+     discardSavedDocument();
+   }
+ })();

  // step 2: 加载配置
  loadContentConfig().then(() => {
      store._emit();
  });

  // step 3: 拖放导入
  io.setupDropZone(document.body, json => {
    store.loadChapter(json);
+   setFileNameFromJson(json);
  }, (msg) => showAlert(msg));

  // step 4: 初始化界面
  initUI(store, io);

+ // step 5: 注册自动保存
+ start(store);
+ store.onChange(() => { notifyChange(); });

+ // step 6: 导入/导出时更新文件名
+ import('./logic/logic-storyIO.js').then(io2 => {
+   const origExport = io2.exportJSON;
+   io2.exportJSON = (json, filename) => {
+     discardSavedDocument();
+     origExport(json, filename);
+   };
+ });
+
+ function setFileNameFromJson(json) {
+   import('./logic/logic-autoSave.js').then(m => {
+     m.setFileName(json?.meta?.name ? (json.meta.name + '.json') : 'imported.json');
+   });
+ }
+
  // step 5（旧）：开发调试 URL 参数
  const params = new URLSearchParams(window.location.search);
  const sampleUrl = params.get('load');
  if (sampleUrl) {
      fetch(sampleUrl)
          .then(r => r.json())
          .then(json => {
            store.loadChapter(json);
+           setFileNameFromJson(json);
          })
          .catch(() => console.log('示例文件加载失败，请拖入 JSON 文件开始'));
  }
```

注意：原始的 step 5 变成了 step 7（URL 参数加载）。

---

### 任务 10：更新 `js/barrel.js` — 导出新模块

**文件：** 修改 `js/barrel.js`

**说明：** 导出 `logic-migration.js` 和 `logic-autoSave.js` 中必要的函数。

- [ ] **步骤 1：添加导出**

```diff
+ // 迁移 & 存储层
+ export { runMigration, readConfig, writeConfig, readSchema, writeSchema, readDocument, writeDocument, clearDocument,
+          CONFIG_KEY, SCHEMA_KEY, DOCUMENT_KEY } from './logic/logic-migration.js';
+ 
+ // 自动保存
+ export { start, stop, notifyChange, getStatus, onStatusChange, hasSavedDocument, getSavedDocument, discardSavedDocument } from './logic/logic-autoSave.js';
```

---

### 任务 11：创建 `components/AutoSaveIndicator.vue`

**文件：** 创建 `components/AutoSaveIndicator.vue`

**说明：** 显示在 ActivityBar 底部的状态指示器，展示 idle/saving/saved/error 四种状态。使用 Pinia 的 autoSaveStatus。

- [ ] **步骤 1：创建组件**

```vue
<template>
  <div class="auto-save-indicator" :class="statusClass" :title="tooltip">
    <span v-if="status === 'saving'" class="asi-icon">💾</span>
    <span v-else-if="status === 'saved'" class="asi-icon">✓</span>
    <span v-else-if="status === 'error'" class="asi-icon">⚠</span>
    <span class="asi-text">{{ label }}</span>
  </div>
</template>

<script setup>
import { computed } from 'vue'
import { useStoryStore } from '../../stores/storyStore.js'

const store = useStoryStore()
const status = computed(() => store.autoSaveStatus)

const statusClass = computed(() => `asi-${status.value}`)

const label = computed(() => {
  switch (status.value) {
    case 'saving': return '保存中…'
    case 'saved':  return '已自动保存'
    case 'error':  return '保存失败'
    default:       return ''
  }
})

const tooltip = computed(() => {
  switch (status.value) {
    case 'saving': return '正在自动保存…'
    case 'saved':  return '已自动保存到本地'
    case 'error':  return '自动保存写入失败'
    default:       return ''
  }
})
</script>

<style scoped>
.auto-save-indicator {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 4px 8px;
  font-size: 0.75rem;
  color: var(--text-dim);
  cursor: default;
  user-select: none;
  border-top: 1px solid var(--border);
  min-height: 24px;
}
.asi-icon {
  font-size: 0.7rem;
  line-height: 1;
}
.asi-text {
  font-size: 0.7rem;
  line-height: 1;
}
.asi-saved {
  color: var(--success);
}
.asi-error {
  color: var(--accent);
}
.asi-saving .asi-icon {
  animation: asi-spin 1s linear infinite;
}
@keyframes asi-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
</style>
```

---

### 任务 12：更新 `components/layout/ActivityBar.vue` — 底部添加指示器

**文件：** 修改 `components/layout/ActivityBar.vue`

**说明：** 在活动栏按钮下方添加 `AutoSaveIndicator` 组件。

- [ ] **步骤 1：引入 AutoSaveIndicator 并在底部渲染**

```diff
  <template>
    <div class="activity-bar" id="activity-bar">
+     <div class="activity-bar-buttons">
        <div v-for="item in items" ...>
          ...
        </div>
+     </div>
+     <AutoSaveIndicator />
    </div>
  </template>

  <script setup>
  import { ref } from 'vue'
+ import AutoSaveIndicator from '../AutoSaveIndicator.vue'
  // ... 其余代码不变 ...
  </script>

+ <style scoped>
+ .activity-bar {
+   display: flex;
+   flex-direction: column;
+   justify-content: space-between;
+ }
+ .activity-bar-buttons {
+   display: flex;
+   flex-direction: column;
+ }
+ </style>
```

注意：```<div class="activity-bar" id="activity-bar">``` 的 CSS 需要加上 `display: flex; flex-direction: column;` 让按钮区域在上方、自动保存指示器在底部。

---

### 任务 13：更新 `components/App.vue` — 确认 ActivityBar 带指示器

**文件：** 修改 `components/App.vue`

**说明：** 确认 `ActivityBar` 已包含 `AutoSaveIndicator`，无需额外改动。只需确认构建无报错。

- [ ] **步骤 1：验证 App.vue 无变动需要**

检查 `App.vue` 中 `<ActivityBar />` 的使用方式是否兼容（ActivityBar 内部已包含 AutoSaveIndicator，外层不需要改）。

---

### 任务 14：构建验证

**说明：** 运行 Vite 构建确认无编译错误。

- [ ] **步骤 1：运行 `npx vite build` 并确认无报错**

运行：`cd d:\_Program\MyOpenSource\json-management-control && npx vite build`

预期：Build 成功，无 error

- [ ] **步骤 2：运行 `npx vite` 启动 dev server，在浏览器中验证**

运行：`cd d:\_Program\MyOpenSource\json-management-control && npx vite`

预期：http://localhost:5173 正常打开，ActivityBar 底部可见自动保存指示器
